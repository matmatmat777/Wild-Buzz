<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.wcs.wildbuzz">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true"/>
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".List" />
        <activity android:name=".CreateAccount" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ChallengeA" />
        <activity android:name=".Scanner"/>
        <activity android:name=".ChallengeB" />
        <activity android:name=".ChallengeC" />
        <activity android:name=".Profil" />
        <activity android:name=".Rank" />
        <activity android:name=".DrawingMainActivity" />
        <activity android:name=".ChallengeAplay" />
        <activity android:name=".ChallengeBSecondePage" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>